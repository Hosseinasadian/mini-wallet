apiVersion: v1
kind: Service
metadata:
  name: wallet-docs-service
spec:
  type: ClusterIP
  selector:
    app: wallet-docs
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.docs.ports.service }}
      targetPort: {{ .Values.docs.ports.container }}