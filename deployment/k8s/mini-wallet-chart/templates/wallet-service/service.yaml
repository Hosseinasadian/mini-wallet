apiVersion: v1
kind: Service
metadata:
  name: wallet-wallet-service
spec:
  type: ClusterIP
  selector:
    app: wallet-wallet
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.wallet.ports.service }}
      targetPort: {{ .Values.wallet.ports.container }}